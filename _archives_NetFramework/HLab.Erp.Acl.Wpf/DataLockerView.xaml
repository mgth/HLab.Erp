<UserControl x:Class="HLab.Erp.Acl.Wpf.DataLockerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HLab.Erp.Acl.Wpf"
             xmlns:icons="clr-namespace:HLab.Mvvm.Wpf.Icons;assembly=HLab.Mvvm.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ToggleButton IsChecked="{Binding IsActive}">
            <icons:IconView>
                <icons:IconView.Style>
                <Style TargetType="{x:Type icons:IconView}">
                    <Style.Triggers>
                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                        <Setter Property="Id" Value ="icons/Locked"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                        <Setter Property="Id" Value ="icons/Unlocked"/>
                    </DataTrigger>
                </Style.Triggers>
                </Style>
                </icons:IconView.Style>
            </icons:IconView>
    </ToggleButton>
</UserControl>
